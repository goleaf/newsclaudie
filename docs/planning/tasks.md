# Tasks (Relocated)

This file keeps project task notes that previously lived at the repository root.

The original `tasks.md` content was not present when the documentation structure was reorganized. Add or restore the current task list here so planning notes stay under `docs/planning/`.
