# TODO (Relocated)

This placeholder keeps the todo list under `docs/planning/`.

If you had items in the former root `todo.md`, copy them here so the planning backlog remains centralized inside the documentation tree.
